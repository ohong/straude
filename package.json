{
  "name": "straude-monorepo",
  "private": true,
  "packageManager": "bun@1.3.3",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "silo": "bun scripts/knowledge-silo.ts",
    "doc-drift": "bun scripts/doc-drift.ts"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5"
  },
  "dependencies": {
    "motion": "^12.34.3",
    "web": "^0.0.2"
  }
}
